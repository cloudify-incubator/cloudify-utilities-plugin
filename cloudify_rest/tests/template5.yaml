rest_calls:
  - path: /v1/get_response5
    method: GET
    response_format: Xml
    response_translation:
        response:
          result:
            system:
              vm-uuid: ['UUID']
              vm-cpuid: ['CPUID']
