plugins:
  # Name could be anything, this name is what appears on the beginning of operation
  # mappings.
  deployment:
    executor: central_deployment_agent
    source: https://github.com/denismakogon/cloudify-deployment-proxy-plugin/archive/master.zip

node_types:
  cloudify.nodes.DeploymentProxy:
    derived_from: cloudify.nodes.Root
    properties:
      deployment_id:
        default: ''
      inherit_outputs:
        default: []
      timeout:
        default: 30
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: deployment.proxy.tasks.wait_for_deployment
        start:
          implementation: deployment.proxy.tasks.obtain_outputs
      cloudify.interfaces.validation:
          creation:
              implementation: deployment.proxy.tasks.creation_validation
